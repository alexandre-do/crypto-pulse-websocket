# Aggregator

TypeScript Kafka consumer that computes aggregated metrics (OHLCV, volume, etc.) from trade and order book events and persists them to PostgreSQL. See the root [README](../../README.md#architecture) for how this fits into the pipeline.
