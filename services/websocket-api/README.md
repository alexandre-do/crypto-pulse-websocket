# WebSocket API

TypeScript service that exposes live trades, order books, and anomaly alerts to the React dashboard over WebSockets. See the root [README](../../README.md#architecture) for how this fits into the pipeline.
