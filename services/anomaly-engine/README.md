# AI / Anomaly Engine

Python service that analyzes stored and streamed market data to detect anomalies (price spikes, volume surges, spoofing patterns, etc.) and feeds alerts to the WebSocket API. See the root [README](../../README.md#architecture) for how this fits into the pipeline.
